include ./article-sort.pug
mixin slideArticle()
  - var posts = site.posts.filter(item => item.layout === 'slides')
  #archive
    .article-sort-title= 'Slides - ' + posts.length
    +articleSort(posts)

mixin slide(item)
  section(data-markdown
    data-separator=theme.slide.separator
    data-separator-vertical=theme.slide.separtor_vertical
    data-charset=theme.slide.charset)
      script(type="text/template")!= item._content
